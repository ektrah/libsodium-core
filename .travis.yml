language: c
os:
 - osx
 - linux
install:
 - if [ "${TRAVIS_OS_NAME}" = "linux" ]; then .travis-linux-deps.sh; fi
 - if [ "${TRAVIS_OS_NAME}" = "osx" ]; then .travis-osx-deps.sh; fi
before_script:
 - ./travis-build-libsodium.sh
script:
 - xbuild libsodium-net.sln
 - nunit-console ./Tests/bin/Debug/Tests.dll
